<div class="container">
  <h1><%= @dream.name %></h1>
  <p><%= @dream.description %></p>
  <p><%= cl_image_tag @dream.photo.key %></p>
  <p>Price: £<%= @dream.price  %></p>
  <p>Duration: <%= @dream.duration %> hours</p>
  <p>Created by <%= @dream.user.first_name + ' ' + @dream.user.last_name%></p>

  <% link_to "Edit", edit_dream_path, class: "btn btn-secondary" %>
  <% link_to "Delete", "/dreams/#{params[:id]}", method: :delete, class: "btn btn-danger" %>

  <%= render "bookings/form", locals: {dream: @dream, booking: @booking} %>
</div>
